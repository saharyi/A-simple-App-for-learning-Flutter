import 'package:flutter/material.dart';
import 'package:fl_chart/fl_chart.dart';

class Charts extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
        child: Text(
            "متاسفانه این آپشن هنوز در نسخه وب فلاتر پیاده سازی نشده است :(")
        /*PieChart(
        PieChartData(
          sections: [
            PieChartSectionData(color: Colors.red, value: 3),
            PieChartSectionData(color: Colors.blue, value: 3),
            PieChartSectionData(color: Colors.yellow, value: 3),
            PieChartSectionData(color: Colors.green, value: 3),
          ],
        ),
      ),*/
        );
  }
}
